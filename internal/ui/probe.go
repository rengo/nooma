package ui

// probe.go is a scratch file that exists only to prove the ui-boundary
// depguard rule fails before anything else in internal/ui does — it is
// deleted in the GREEN commit that follows this one.
import _ "github.com/rengo/nooma/internal/store"
